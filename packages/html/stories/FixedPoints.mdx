import { Meta } from '@storybook/blocks';
import * as story from './FixedPoints.stories';

<Meta of={story} />

# Fixed Points

This example demonstrates using fixed connection points for connecting edges to vertices.
