// TODO should be import only were required
import '@maxgraph/core/css/common.css';

// TODO rename file

export const globalTypes = {
  width: {
    type: 'number',
    defaultValue: 800,
  },
  height: {
    type: 'number',
    defaultValue: 600,
  },
};
